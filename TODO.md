# ✅ TODO.md — کارهای باقی‌مانده و برنامه توسعه

> این فایل اولویت‌بندی کارهای آینده پروژه را نشان می‌دهد. بعد از انجام هرکدام، آن را به CHANGELOG.md منتقل کن و از این لیست حذف کن.

---

## 🔴 اولویت بالا — نقشه راه ۷ مرحله‌ای دستور توسعه حرفه‌ای (فعال)

طبق دستور «توسعه حرفه‌ای سامانه جامع مدیریت و تبلیغات آموزشگاه‌های آزاد»، پروژه باید در ۷ مرحله تکمیل شود. وضعیت فعلی:

- [x] **مرحله ۱ — سیستم استوری آموزشگاه‌ها:** ✅ تکمیل و تست شده (نوار استوری صفحه اصلی، مشاهده تمام‌صفحه، پنل مدیریت استوری و عکس پروفایل با برش تصویر)
- [x] **مرحله ۲ — سیستم آپلود حرفه‌ای فایل:** ✅ تکمیل (`useFileUpload` hook با درصد/سرعت/ETA/لغو، به‌کاررفته در استوری و عکس پروفایل و مدارک)
- [x] **مرحله ۳ — مدیریت مدارک هنرجویان:** ✅ تکمیل و تست شده (جدول `student_documents`، CRUD کامل در پنل مدیر، مشاهده/دانلود/چاپ در پنل هنرجو)
- [ ] **مرحله ۴ — سیستم اعلام نمرات:** ⏳ مرحله بعدی پیشنهادی و هنوز شروع نشده (ثبت نمرات در پنل مدیر + کارنامه در پنل هنرجو با معدل، وضعیت قبولی، چاپ/دانلود PDF). طبق قانون پروژه، فقط بعد از تأیید کامل کاربر باید شروع شود.
- [ ] **مرحله ۵ — امکانات حرفه‌ای پنل مدیر آموزشگاه:** ⏳ باقی‌مانده — بخش‌هایی از این مرحله (مدیریت دوره‌ها، ثبت‌نام، تبلیغات) از قبل پیاده شده، اما موارد زیر خیر: مدیریت اساتید (رزومه/تخصص/برنامه تدریس)، مدیریت کلاس‌ها (برنامه هفتگی ساختاریافته)، مدیریت مالی (بدهکاران/فاکتور رسمی)، پیام‌رسان داخلی، مرکز اعلان‌ها، گزارش بازدید
- [ ] **مرحله ۶ — امکانات پنل هنرجو:** ⏳ بخشی پیاده شده (دوره‌ها، مدارک، ثبت‌نام چندگانه) — باقی‌مانده: فاکتورها، تکالیف (مشاهده/ارسال)، تیکت پشتیبانی، ویرایش پروفایل/تغییر رمز، 2FA، تاریخچه فعالیت
- [ ] **مرحله ۷ — امکانات مدیریتی سطح سامانه (مدیر کل):** ⏳ بخشی پیاده شده (مدیریت آموزشگاه‌ها/مدیران/مالی/تنظیمات صفحه اصلی) — باقی‌مانده: مدیریت اشتراک و پلن‌ها، مدیریت نقش‌ها و سطح دسترسی (RBAC واقعی)، مدیریت شکایات/تیکت، اعلان سراسری، لاگ کامل عملیات (Audit Log)

**قانون اجرا:** هر مرحله باید کاملاً تحلیل → طراحی DB → UI → Front-end → Back-end → اتصال API → تست → رفع خطا → تست مجدد شود، سپس با گزارش کامل (قابلیت‌های تکمیل‌شده، تست‌ها، مشکلات، موارد باقی‌مانده) تحویل داده شود و فقط بعد از تأیید کاربر وارد مرحله بعد شویم.

---

## 🟠 اولویت متوسط (پیشنهادی برای تکمیل تجربه فعلی)

- [ ] اتصال پنل پیامکی واقعی (Kavenegar یا مشابه) برای ارسال OTP واقعی به‌جای نمایش کد روی صفحه
- [ ] اتصال درگاه پرداخت آنلاین (زرین‌پال/آیدی‌پی) برای پرداخت شهریه هنگام ثبت‌نام
- [ ] بهبود تابع `slugify` در `src/app/api/manager/route.ts` برای تولید URL استاندارد (لاتین) حتی برای عنوان‌های فارسی
- [ ] افزودن UI ثبت نظر و امتیاز واقعی توسط هنرجو (جدول `reviews` آماده است، فقط UI کم است)
- [ ] صفحه مدیریت رشته‌ها (`categories`) از پنل مدیر کل — فعلاً فقط با seed دستی قابل افزودن است

## 🟡 اولویت پایین (بهبودهای آینده)

- [ ] تولید خودکار PDF گواهینامه/کارنامه با قالب رسمی و نام هنرجو/دوره (به‌جای آپلود دستی تصویر توسط آموزشگاه)
- [ ] جدول اختصاصی `attendance` (حضور و غیاب واقعی) و `weekly_schedule` (برنامه هفتگی ساختاریافته) — فعلاً فقط فیلد متنی `schedule` وجود دارد
- [ ] سیستم اعلان داخل‌سایتی (Notification Center) برای هنرجو و مدیر آموزشگاه
- [ ] گزارش‌ساز سفارشی (Report Builder) برای مدیر کل
- [ ] پشتیبانی از دامنه اختصاصی هر آموزشگاه (Multi-domain)
- [ ] لاگ کامل فعالیت‌های مدیریتی (Audit Log)
- [ ] ورود دومرحله‌ای (2FA) برای مدیر کل و هنرجو
- [ ] بهینه‌سازی ذخیره تصاویر/فایل‌ها (فعلاً base64 داخل دیتابیس ذخیره می‌شود — بهتر است به یک Object Storage مثل S3/Cloudinary منتقل شود تا حجم دیتابیس کنترل شود). این مورد با رشد سامانه به سطح کشوری **حیاتی** خواهد شد.

## 🧪 موارد نیازمند تست بیشتر در آینده

- [ ] تست بار (Load Test) برای همزمانی ثبت‌نام‌های زیاد
- [ ] تست کامل مسیر «فراموشی رمز عبور» (فعلاً چنین قابلیتی پیاده‌سازی نشده)
- [ ] تست مرورگرهای مختلف (Safari، Firefox، Edge) برای کامپوننت‌های جدید (StoryViewer، ImageCropperModal)

## 💡 ایده‌های آینده (از خواسته‌های کلی کاربر که هنوز پیاده نشده)

طبق درخواست‌های تصویری اولیه کاربر (الهام از AryaTehran) که هنوز به‌صورت کامل پیاده نشده‌اند:
- [ ] نقشه حرارتی (Heatmap) عملکرد آموزشگاه‌ها بر اساس منطقه در پنل مدیر کل
- [ ] دستیار هوش مصنوعی برای پیشنهاد دوره به مدیران (AI course recommendation)
- [ ] پیش‌بینی درآمد با تحلیل روند ثبت‌نام
- [ ] سیستم Workflow چندمرحله‌ای تأیید (ثبت‌نام → پرداخت → صدور گواهینامه)
- [ ] سیستم افزونه (Plugin System) برای توسعه بدون تغییر هسته

> نکته: این بخش صرفاً آرشیو ایده‌های بلندمدت است؛ فقط در صورت درخواست صریح کاربر روی این موارد کار شود.
