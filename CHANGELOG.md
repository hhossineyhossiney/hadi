# 📝 CHANGELOG.md

فرمت: جدیدترین تغییرات در بالا. هر ورودی مربوط به یک درخواست/مرحله از گفتگو با کاربر است.

---

## [نسخه فعلی] — تکمیل و تست Production مرحله ۳ (مدیریت مدارک هنرجویان) + تثبیت مرحله ۱
- جدول `student_documents` برای مدیریت حرفه‌ای مدارک هنرجویان ایجاد شد.
- API جدید `/api/manager/documents` ساخته شد: آپلود، حذف، جایگزینی، ویرایش اطلاعات مدرک، ثبت شماره مدرک، سریال، تاریخ صدور، وضعیت اعتبار و توضیحات.
- API جدید `/api/student/documents` ساخته شد تا هنرجو مدارک خود را مشاهده، دانلود و چاپ کند.
- در پنل مدیر آموزشگاه، داخل بخش هنرجویان دکمه «مدیریت مدارک» اضافه شد و مودال کامل مدارک ساخته شد.
- در پنل هنرجو، تب «مدارک من» اضافه شد.
- سیستم استوری آموزشگاه‌ها (مرحله ۱) بعد از اصلاح Schema ناسازگار Neon روی Production مجدداً تست شد و سالم است.
- دیپلوی Production انجام شد و مسیرهای `/api/stories`, `/api/manager/stories`, `/api/manager/documents`, `/api/student/documents` تست شدند.

## رفع خطای ثبت دوره در پنل مدیر آموزشگاه
- بررسی شد که هنگام ثبت دوره جدید، اگر مدیر آموزشگاه در فیلدهای عددی مثل «شهریه» یا «ظرفیت» متن وارد کند، قبلاً خطای عمومی «خطای سرور» نمایش داده می‌شد.
- در `src/app/api/manager/route.ts` اعتبارسنجی حرفه‌ای سمت سرور برای فیلدهای عددی اضافه شد.
- اعداد فارسی/عربی در `price` و `capacity` پشتیبانی می‌شوند و به عدد استاندارد تبدیل می‌شوند.
- خطاهای نامعتبر اکنون فارسی و قابل فهم هستند: «شهریه باید فقط عدد باشد» و «ظرفیت باید فقط عدد باشد».
- تست شد: قیمت نامعتبر، ظرفیت نامعتبر، و ورودی معتبر با اعداد فارسی؛ همه نتیجه صحیح دادند و دیگر خطای ۵۰۰ ایجاد نشد.

## مستندسازی پروژه
- ایجاد فایل‌های `PROJECT.md`, `CHANGELOG.md`, `TODO.md`, `DATABASE.md`, `API.md`
- ایجاد صفحه زنده `/project-memory` داخل سایت که خلاصه همین مستندات را نمایش می‌دهد
- این فایل‌ها از این پس بعد از هر پاسخ مهم به‌روزرسانی می‌شوند

## بنر اسلایدی آموزشگاه + بنر تبلیغاتی دوره
- افزودن ستون `bannerImages` به جدول `institutes` (اسلایدر ۳+ اسلاید در صفحه آموزشگاه)
- افزودن ستون `bannerImages` به جدول `courses` (بنر تبلیغاتی در صفحه جزئیات دوره)
- ساخت کامپوننت `InstituteBannerSlider.tsx` و `CourseBannerSlider.tsx` (چرخش خودکار)
- افزودن اکشن‌های `addBanner`/`deleteBanner` (آموزشگاه) و `addCourseBanner`/`deleteCourseBanner` (دوره) به `/api/manager`
- افزودن مدیریت بنر دوره در تب «مدیریت دوره‌ها»ی پنل آموزشگاه (دکمه بنفش کنار هر دوره)
- رفع باگ جهت Sidebar: تغییر `flex-row-reverse` به `flex-row` در `/admin` و `/panel` تا نوار کناری در RTL سمت راست قرار بگیرد

## بازگردانی کامل پنل مدیر آموزشگاه
- کاربر گزارش داد نسخه قبلی پنل مدیر آموزشگاه (با امکانات ثبت دوره، هنرجویان، گالری) در یک بازنویسی حذف شده بود
- بازسازی کامل `/panel` با طراحی تیره (مطابق پنل مدیر کل) و ۶ تب: داشبورد، مدیریت دوره‌ها (create/update/delete)، لیست هنرجویان (+ آپلود گواهینامه)، گالری، بنر اسلایدی، تلگرام
- افزودن ستون `certificateUrl` به جدول `registrations`
- افزودن اکشن‌های `createCourse`, `deleteCourse`, `uploadCertificate` به `/api/manager`

## ساخت پنل مدیر کل سامانه (Super Admin) کامل
- طبق درخواست تصویری کاربر (شبیه AryaTehran)، بازطراحی کامل `/admin` با Sidebar تیره سمت راست و ۷ تب
- افزودن جدول `site_settings` برای کنترل محتوای صفحه اصلی توسط مدیر کل
- API های جدید: `/api/admin/institutes` (CRUD کامل)، `/api/admin/finance`، `/api/admin/settings`
- بازطراحی `/api/admin/stats` با افزودن `todayCount` و `totalRevenue`
- صفحه اصلی (`page.tsx`) به‌روزرسانی شد تا از `site_settings` برای «آموزشگاه‌های برتر» و «دوره‌های ویژه» استفاده کند (fallback به انتخاب خودکار در صورت خالی بودن)
- افزودن `/api/student/enroll` برای ثبت‌نام چندگانه هنرجو بدون فرم/OTP مجدد
- افزودن `/api/student/recommendations` برای نمایش حداکثر ۲ آموزشگاه پیشنهادی در پنل هنرجو
- بازسازی کامل `/dashboard` هنرجو با ۵ تب (داشبورد، دوره‌های من، برنامه هفتگی، گواهینامه‌ها، ثبت‌نام دوره جدید)

## Rollback فیچر استوری اینستاگرامی
- کاربر درخواست بازگشت کامل به نسخه قبل از افزودن قابلیت «استوری شبیه اینستاگرام» را داد
- حذف `StoriesBar.tsx`, `StoryUploader.tsx`, `/api/manager/stories`
- حذف ستون‌های `logoUrl`, `dailyStories`, `isActive` (نسخه قدیمی)، `portfolioImages` از هر دو دیتابیس (لوکال و Neon)
- بازگردانی `globals.css` به پالت رنگی Premium Blue قبلی
- بازگردانی `/panel` به نسخه پایدار قبلی (که بعداً در مرحله بعد کامل‌تر بازسازی شد)

## بازطراحی Ultra Luxury + برندینگ کامل
- تغییر نام رسمی سایت به «آموزشگاه‌های آزاد مرکز فنی و حرفه‌ای شهرستان زبرخان — مرکز شماره ۱۲»
- افزودن اطلاعات سازنده (مهندس سیدحمید حسینی — ۰۹۱۵۹۵۱۳۱۷۹) در فوتر
- حذف تک‌حرف («آ») از همه کارت‌ها؛ جایگزینی با هدر گرادیانی رنگی + متن کامل «آموزشگاه آزاد فنی و حرفه‌ای»
- ساخت `src/lib/cardTheme.ts` برای تعیین خودکار گرادیان رنگی بر اساس رشته/نام آموزشگاه
- افزودن `QuickAccess.tsx` (۴ باکس دسترسی سریع)
- تولید تصاویر AI برای پس‌زمینه Hero و کاور آموزشگاه‌ها
- افزودن `MobileNav.tsx` (نوار پایین موبایل)

## سیستم OTP + ثبت‌نام چندمرحله‌ای + مدیران آموزشگاه (فاز اول)
- افزودن جدول `otp_codes` و API `/api/otp` (ارسال/تأیید کد ۵ رقمی، انقضا ۳ دقیقه)
- بازسازی `/register` به Wizard ۴ مرحله‌ای با اعتبارسنجی شماره موبایل (regex `09XXXXXXXXX`) و تأیید دوباره رمز عبور
- افزودن ستون `accessCode` به `institutes` برای اتصال ربات تلگرام اختصاصی هر آموزشگاه
- افزودن جدول `telegram_chats` با فیلد `instituteId` برای تفکیک اعلان‌ها
- ساخت اولین نسخه `/panel` (پنل مدیر آموزشگاه) و `/api/manager`
- ساخت اولین نسخه `/api/admin/managers` برای ساخت حساب مدیران توسط مدیر کل

## اتصال ربات تلگرام
- دریافت توکن ربات از کاربر و تست موفق (`@amoozeshghah_bot`)
- ساخت `src/lib/telegram.ts` (ارسال پیام + فرمت اعلان)
- ساخت `/api/telegram/webhook` با دستورات فارسی (گزارش، آمار، لیست آموزشگاه/دوره، راهنما)
- ساخت `/api/telegram/setup` برای تنظیم Webhook
- شناسایی و ثبت chatId مدیر کل (مهندس سیدحمید حسینی) به‌عنوان دریافت‌کننده پیش‌فرض همه اعلان‌ها

## رفع خطای Vercel Build (DATABASE_URL)
- خطای Build به دلیل throw شدن exception در `src/db/index.ts` هنگام نبود `DATABASE_URL` در مرحله Build
- بازنویسی `src/db/index.ts` با الگوی Proxy تنبل (Lazy) که فقط در زمان درخواست واقعی به دیتابیس متصل می‌شود
- افزودن `export const dynamic = "force-dynamic"` به همه صفحات و API هایی که از دیتابیس استفاده می‌کنند

## اتصال دیتابیس ابری Neon + Deploy روی Vercel
- دریافت Connection String Neon از کاربر، اجرای `drizzle-kit push` و `seed.ts` روی آن
- دریافت توکن شخصی Vercel از کاربر و دیپلوی مستقیم توسط دستیار (`vercel deploy --prod`)
- تنظیم Environment Variables در Vercel: `DATABASE_URL`, `NEXTAUTH_SECRET`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_ADMIN_CHAT_ID`
- رفع خطای `CredentialsSignin` (رمزهای عبور بین لوکال/Neon هماهنگ نبودند) با ریست دستی هش bcrypt روی هر دو دیتابیس

## نسخه اولیه پلتفرم (MVP)
- ساخت Schema اولیه: `users`, `categories`, `regions`, `institutes`, `courses`, `registrations`, `reviews`
- Seed داده‌های واقعی ۱۸ آموزشگاه شهرستان زبرخان (خیاطی، زیبایی، کامپیوتر، آشپزی، زبان) از لیست ارائه‌شده توسط کاربر
- ساخت صفحات اصلی: Landing, Institutes, Courses, Fields, Search, Register, Login
- ساخت Design System اولیه (چند بار بازطراحی رنگ در مراحل بعد)
- پیاده‌سازی خروجی Excel با کتابخانه `xlsx`
- پیاده‌سازی احراز هویت NextAuth با شماره موبایل
